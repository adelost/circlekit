package com.adelost.renderkit.list

/** The Wear theme injected body1's 20.sp line height into every `Text`;
 *  ported text pins it explicitly so WatchExact stays exact. */
const val WATCH_TEXT_LINE_HEIGHT_SP = 20f

/** Wear body1's letterSpacing, inherited wherever the original screen did
 *  not pass its own value. */
const val WATCH_TEXT_LETTER_SPACING_SP = 0.18f
